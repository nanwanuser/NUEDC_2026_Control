# 扑克牌拼图策略实施计划

## 目标

新增独立的扑克牌拼图策略：按键1保持现有几何策略，按键2接收MaixCAM输出的几何与花纹特征，使用几何约束和花纹评分选择正确牌面后复用现有运动执行链。

## 阶段

| 阶段 | 状态 | 验收条件 |
|---|---|---|
| 1. 计划与接口冻结 | complete | 数据模型、策略分流和测试入口明确 |
| 2. STM32卡牌决策核心 | complete | 四个几何相同碎片能由特征评分选出正确排列；Key1回归通过 |
| 3. 卡牌串口协议 | complete | 分片卡牌帧能组装、校验、稳定后提交 |
| 4. MaixCAM花纹特征 | complete | 可从合成帧输出边缘事件和内部基元；实拍调参待上板 |
| 5. 任务层集成 | complete | Key2只启动卡牌策略，Key1行为不变 |
| 6. 全量验证与文档 | in_progress | C测试、Python测试和可运行构建全部通过 |

## 范围约束

- 不修改按键1现有几何策略的行为。
- 不使用OCR、神经网络或固定52张牌模板。
- MaixCAM只提取特征，不执行组合搜索。
- STM32只在完整几何候选上计算整体花纹评分。
- 花纹证据不足时返回明确的歧义状态，不把第一个几何矩形当作正确牌面。

## 遇到的错误

| 错误 | 尝试次数 | 处理 |
|---|---:|---|
| 捆绑Python缺少cv2 | 1 | 用户授权后在MaixCAM工程 `.venv` 安装并验证OpenCV、NumPy、pytest |
| 并行环境检查超时 | 1 | 后续改为较小的独立命令 |
| 卡牌协议CRC测试失败 | 1 | 测试序列化与分包使用了不同seq；统一输入并补充seq校验 |
