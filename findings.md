# 关键发现

- 题目现场碎片不超过4片，每片不超过5边；扑克牌任务要求相邻花纹对应。
- `Task/mission/mission.c` 将Key1绑定为 `MISSION_GEOMETRIC`、Key2绑定为 `MISSION_CARD_PATTERN`；`build_request()` 已据此设置独立决策策略。
- 当前视觉协议只传碎片ID、中心和顶点；原decision_mode字段已作为reserved忽略。
- 当前几何搜索在分数小于 `DECISION_GOOD_ENOUGH_SCORE` 后提前退出；几何相同的四等分碎片会选择搜索到的第一个矩形。
- 卡牌模式只在全部可靠边缘事件成对匹配时提前退出，否则在每个完整合法候选上计算花纹分数。
- 特征模型采用切口事件和内部基元；纯白接缝只提供中性证据。
- 当前串口单帧上限118字节、接收环512字节，卡牌特征需要分片。
- 卡牌块使用95字节分片、整包CRC16和layout_id；同一冻结结果重复三轮后才提交。
- MaixCAM只在布局冻结时提取一次花纹，并按1个几何帧、2个卡牌分片交错发送。
- 标准四等分卡牌回归只访问5个搜索节点；实机CPU耗时仍需上板测量。
- Debug固件当前RAM占用约46KB/128KB、Flash约98KB/512KB。
