# 实施进度

## 2026-08-01

- 完成题目、STM32工程和MaixCAM工程只读分析。
- 用户确认新增独立策略：Key1保持现有策略，Key2执行扑克牌策略。
- 冻结首版范围：结构化花纹特征由MaixCAM提取，组合搜索由STM32完成。
- 创建持久计划文件；下一步编写STM32卡牌决策失败测试。
- 建立两个仓库的 `codex/card-pattern-strategy` 分支。
- RED：新增四等分卡牌特征测试，因卡牌类型和API不存在而编译失败。
- GREEN：新增 `DecisionCardFrame`、切口事件、内部基元和 `Decision_SolveCard()`；卡牌模式关闭几何提前退出并对完整接缝评分。
- 验证：`ctest --test-dir build/decision-tests --output-on-failure`，1/1通过，总耗时6.58秒。
- 性能观察：完整枚举在主机上约6.6秒，后续需要分支排序/剪枝并进行STM32计时。
- 用户授权安装依赖；在MaixCAM工程 `.venv` 安装并验证OpenCV 5.0.0、NumPy 2.5.1、pytest 9.1.1。
- 完成卡牌分片协议：乱序、缺片等待、整包CRC失败复位及恢复测试通过。
- 完成MaixCAM红/黑连通域特征提取；边事件的位置、方向、宽度统一在标定毫米平面计算。
- 完成双协议冻结发布：几何帧供Key1，重复卡牌分片供Key2，互不改变对方接收状态。
- 完成Key2任务分流、卡牌组包三次稳定提交及 `Decision_SolveStrategy()` 调度。
- 加入证据优先搜索、冲突接缝剪枝和全可靠事件提前结束；标准卡牌用例搜索节点为5。
- 修复负有向面积多边形规范化时事件边号和沿边位置未同步的潜在问题。
- 验证：MaixCAM `pytest` 6/6通过，Python语法编译通过；decision与vision_protocol C测试通过。
- Debug固件成功链接：RAM 46112B/128KB（35.18%），Flash 98372B/512KB（18.76%）。
- 最终固件链接：RAM 46120B/128KB（35.19%），Flash 99724B/512KB（19.02%）。
- ARM `-fstack-usage` 复核：Decision任务最深调用估算低于8KB栈，Vision UART任务静态帧1184B低于3KB栈。
- 全部本次相关回归通过；既有 `crane_control_test` 仍失败（完成时偏航速度0 RPM，测试期望40 RPM），未修改该无关模块。
- 未验证：真实扑克牌在现场光照下的阈值、MaixCAM单次特征耗时、STM32实机求解耗时、完整机械流程。
- 完成真实扑克牌四等分主机仿真：默认红桃K图片被规范化为100x70mm并切成四个50x35mm象限，从中心切线提取配对红/黑纹理事件和内部基元，再交给原生 `Decision_SolveCard()` 搜索。
- 仿真保持C算法为唯一决策实现；Python仅负责图像输入、ctypes ABI、场景构造、纹理重建验证和可视化，未复制STM32布局搜索。
- 仿真结果：四条目标相邻关系全部正确，搜索3399节点（上限400000的0.85%），对齐RMS 2.000mm，纹理MAE 13.160/255；最终截图为 `build/card-quadrant-simulation.png`。
- 新增 `--scenario card`、`--card-image` 参数；`--scenario general` 仍为默认并生成回归截图。
- 独立审查后收紧验证：纹理重建仅做一次全局刚体对齐，再按固件导出的2mm径向间隙模型还原；拓扑改为检查边平行度、有向间距、有效重叠及每片径向间隙方向。
- 自定义牌图现在要求四条内部接缝均存在成对花纹证据；无证据、拓扑错误、对齐/纹理误差超限或搜索节点超预算时CLI返回非零。
- RGBA牌图先合成白底，避免透明圆角变成伪黑色特征；OpenCV延迟到card路径导入，默认general场景保持旧依赖边界。
- 验证：仿真测试17/17（含稀疏纹理下向内重叠反例）、decision C测试1/1、vision_protocol C测试1/1通过，Debug固件无需重编且构建成功。
- 仿真结论：在理想俯视、已完成毫米标定且四等分切口工整的前提下，稀疏切线花纹证据足以排除“矩形正确但花纹错误”的组合。
- 仍需上机验证：现场光照下红/黑阈值与反光鲁棒性、真实切割/遮挡造成的断点、MaixCAM到STM32分片串口、STM32求解时间和机械落放误差。
