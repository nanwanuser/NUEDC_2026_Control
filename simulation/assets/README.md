# Playing-card simulation asset

`playing_card.png` is the King of Hearts image downloaded from:

`https://deckofcardsapi.com/static/img/KH.png`

It is stored locally so the simulator and tests run without network access. The
simulation rotates and resizes it at runtime; the original downloaded bitmap is
kept unchanged.
